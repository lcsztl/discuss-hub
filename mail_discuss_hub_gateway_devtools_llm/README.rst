=====================================
Discuss Hub Gateway Devtools for LLM
=====================================

Optional LLM-specific diagnostics for ``mail_discuss_hub_gateway_devtools``.

This addon keeps Gateway AI tracing separate from the base devtools module so
the base tooling can be installed without ``mail_gateway_llm``.
